window.onload=function(){

let precio, cantidad, resultado;

precio = +(prompt("Mete el precio"));
cantidad = +(prompt("Mete la cantidad"));

resultado =  precio * cantidad;

console.log(resultado)

}